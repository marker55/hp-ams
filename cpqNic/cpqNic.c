/*
 * Note: this file originally auto-generated by mib2c using
 *        : mib2c.old-api.conf 14476 2006-04-18 17:36:51Z hardaker $
 */


#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/data_access/interface.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* NOTE: fix this include later.... */
#include "data_access/nic_linux.h"
#include "cpqNic.h"
#include "hpHelper.h"
#include "cpqNicIfLogMapTable.h"
#include "cpqNicIfPhysAdapterTable.h"

extern unsigned char cpqHoMibHealthStatusArray[];

extern  int   InitLibCpqNic(void);
extern  unsigned long iGetIfLastChange(char *);

char NullMAC[IFLOGMAPMACADDRESS_LEN] = {0,0,0,0,0,0};

/*
 * cpqNicMib_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid             cpqNicMib_oid[] = { 1, 3, 6, 1, 4, 1, 232, 18, 1 };
size_t          cpqNicMib_oid_len = OID_LENGTH(cpqNicMib_oid);
int             numIfLog = 0;
int             numIfPhys = 0;
/*
 * variable7 cpqNic_variables:
 *   this variable defines function callbacks and type return information 
 *   for the cpqNic mib section 
 */

#define CPQNICMIBREVMAJOR		1
#define CPQNICMIBREVMINOR		2
#define CPQNICMIBCONDITION		3

/** Initializes the cpqNic module */
void init_cpqNic(void)
{
    DEBUGMSGTL(("cpqNic", "Initializing\n"));

    cpqHostMibStatusArray[CPQMIB].major = CPQMIBREVMAJOR;
    cpqHostMibStatusArray[CPQMIB].minor = CPQMIBREVMINOR;
    cpqHostMibStatusArray[CPQMIB].cond = MIB_CONDITION_OK;

    cpqHoMibHealthStatusArray[CPQMIBHEALTHINDEX] = MIB_CONDITION_OK;

    //netsnmp_access_interface_init();
    /* initialize MIBII/interfaces */
    //init_sysORTable();
    //init_ifTable();

    //init_at();

    //init_icmp();

    //init_ip_scalars();
    //init_inetNetToMediaTable();
    //init_ipAddressPrefixTable();
    //init_ipAddressTable();
    //init_ipDefaultRouterTable();
    //init_ipIfStatsTable();
    //init_ipSystemStatsTable();
    //init_ipv6ScopeZoneIndexTable();
    //init_ip();

    //init_ipv6();

    //init_tcp();
    //init_tcpListenerTable();
    //init_tcpConnectionTable();
    //init_tcpTable();

    //init_udp();
    //init_udpEndpointTable();
    //init_udpTable();

    initialize_table_cpqNicIfPhysAdapterTable();
    initialize_table_cpqNicIfLogMapTable();

    /*
     * register ourselves with the agent to handle our mib tree 
     */
    netsnmp_register_scalar_group(
        netsnmp_create_handler_registration("cpqNic", cpqNic_handler,
                             cpqNicMib_oid, cpqNicMib_oid_len,
                             HANDLER_CAN_RONLY),
                CPQNICMIBREVMAJOR, CPQNICMIBCONDITION);
    cpqHostMibStatusArray[CPQMIB].stat = MIB_STATUS_AVAILABLE;
}

int
cpqNic_handler(netsnmp_mib_handler          *handler,
               netsnmp_handler_registration *reginfo,
               netsnmp_agent_request_info   *reqinfo,
               netsnmp_request_info         *requests)
{
    long val = 0;

    switch (reqinfo->mode) {
    case MODE_GET:
        switch (requests->requestvb->name[ reginfo->rootoid_len - 2 ]) {
            case CPQNICMIBREVMAJOR:
                val = CPQMIBREVMAJOR;
                break;
            case CPQNICMIBREVMINOR:
                val = CPQMIBREVMINOR;
                break;
            case CPQNICMIBCONDITION:
                val = cpqHoMibHealthStatusArray[CPQMIBHEALTHINDEX];
                break;
            default:
                /*
                 * An unsupported/unreadable column (if applicable)
                 */
                snmp_log(LOG_ERR, "unknown object (%lu) in cpqNic_handler\n",
                     requests->requestvb->name[ reginfo->rootoid_len - 2 ]);
                netsnmp_set_request_error( reqinfo, requests,
                                            SNMP_NOSUCHOBJECT );
                return SNMP_ERR_NOERROR;
        }
        snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                 (u_char *)&val, sizeof(val));
        break;

    default:
        snmp_log(LOG_ERR, "unknown mode (%d) in cpqNic_handlery\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;

}

